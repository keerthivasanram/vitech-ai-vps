# Python
backend/.venv/
__pycache__/
*.pyc
.env
backend/.env
backend/chroma_store/
backend/uploads/
backend/data/bulk/
backend/_*.pdf

# Node
frontend/node_modules/
frontend/dist/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# local tooling
.claude/

# Backup bundle: contains the Flowise encryption key and the deploy SSH key.
# Download it, then delete it. It must NEVER be committed.
vitech-critical-backup-*.tar.gz

# Generated engineering packages (regenerable from the requirement)
backend/data/packages/

# Auth database — password hashes, session and service-key hashes, audit trail.
backend/data/auth.db
backend/data/auth.db-*

# Operations database, structured logs and stored artifacts.
backend/data/ops.db
backend/data/ops.db-*
backend/data/jobs/
backend/logs/

# The author's own contact block for the Vitech knowledge request. Not
# engineering, and a company's contact details do not belong in the repo.
docs/sender.json
